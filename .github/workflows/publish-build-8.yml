name: Publish Build 8

on:
  push:
    tags:
      - build-8

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Publish verified pre-release
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          gh release create "$GITHUB_REF_NAME"
          release-assets/Sabaq-Workspace-Build-8.apk
          --title "Sabaq Workspace — Build 8"
          --notes-file release-assets/RELEASE-NOTES-BUILD-8.md
          --prerelease
          --verify-tag
